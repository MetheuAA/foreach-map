/*
Questão 05 – Crie um algoritmo usando o método map() para criar um novo 
array com o dobro de cada número ímpar em um array original?*/

const array = [1, 3, 5, 7, 9]
const soma = array.map((map) => x * 2);
console.log(soma);